/* =====================================================================
 *  기억 회상 지원의 첫 말
 *
 *  통합 시스템 프롬프트 5번이 첫 문장을 정해 두었다. 그대로 쓴다.
 *  사진에 무엇이 보이는지 짚는 말은 넣지 않는다 — 문서 5번은 첫 질문에서
 *  인물 · 관계 · 장소 · 사건을 추측하거나 단정하지 말라고 한다.
 *  사진을 보고 건네는 단서는 대화가 시작된 뒤 문서 15번이 정한 차례에 나간다.
 *
 *  화면(app.js)과 시험(lib/reply.test.mjs)이 함께 쓴다.
 * ===================================================================== */

/** 문서 5번의 첫 고정 질문 (lib/reply-rules.js 의 RECALL_OPENING 과 같은 문장) */
export const RECALL_QUESTION = '이 사진을 보니 어떤 기억이 떠오르시나요?';

/** 문서 5번이 정한 첫 말 그대로 */
export function openingLine() {
  return `사진이 잘 등록됐어요. ${RECALL_QUESTION}`;
}
